import { CurrencyOnlyDirective } from './currency-only.directive';

describe('CurrencyOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
